//Global Object
// In browser we have window object which is attached to the browser but in nodejs we have global object to perform the the functions.
// In node we cannot access DOM objects
// console.log(global);

// setTimeout(() => {
//   console.log("Hi inder");
// }, 2000);

console.log(__dirname);
console.log(__filename);
